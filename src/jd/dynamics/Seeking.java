package jd.dynamics;

import jd.controlling.DynamicPluginInterface;

public class Seeking extends DynamicPluginInterface {

    @Override
    public void execute() {

        // try {
        // Thread.sleep(10000);
        // } catch (final InterruptedException e1) {
        // e1.printStackTrace();
        // }
        // final String id = "3";
        // if (SubConfiguration.getConfig("seeking").getGenericProperty(id,
        // false) && JDInitFlags.SWITCH_DEBUG == false) {
        // // Schon durchgefuehrt
        // return;
        // }
        // SubConfiguration.getConfig("seeking").setProperty(id, true);
        //
        // if
        // (UserIO.isOK(UserIO.getInstance().requestConfirmDialog(UserIO.NO_COUNTDOWN,
        // "We are hiring!",
        // "AppWork is located near Nuremberg in Germany/Bavaria. We're the developers of \r\nJDownloader, the market leading download management tool with over 15 million \r\nhappy users. On top of that, we work on client-side applications for some of the \r\ntop 200 websites worldwide.\r\n\r\nWith only 3 people, we're still a relatively small, but highly motivated team with \r\nhigh aspirations, great opportunities and an extremly optimistic outlook on the \r\nfuture.\r\n\r\nJoining us, you'll have the opportunity to experience rapid growth right when it's \r\nhappening while actively being a part of building and growing a big company.\r\n\r\n",
        // null, "Read more", "Cancel"))) {
        // try {
        // LocalBrowser.openDefaultURL(new
        // URL("http://jdownloader.org/news/blog/x20110707-094307we-hire"));
        // } catch (final Exception e) {
        // e.printStackTrace();
        // }
        // }
        //
        // SubConfiguration.getConfig("seeking").save();
    }

}
